const ResReviewFooter = ({ courseName, level }) => (
  <div className="mt-review-footer">
    <span className="course">🧭 코스: {courseName}</span>
    <span className="level">💪 난이도: {level}</span>
  </div>
);

export default ResReviewFooter;
